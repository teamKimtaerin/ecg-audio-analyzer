# ECG Audio Analysis - GPU-Optimized Dependencies
# CUDA-accelerated libraries for AWS P3/G4 instances

# Include base requirements
-r requirements.txt

# GPU-Accelerated ML Models & Frameworks
torch>=2.0.0           # PyTorch (CUDA version from extra index)
torchaudio>=2.0.0      # GPU audio processing

# Speaker Diarization & Speech Processing (GPU-optimized)
pyannote-audio>=3.0.0  # GPU-accelerated speaker diarization
whisperx                # Main speech recognition + diarization pipeline

# CUDA PyTorch Installation Source
--extra-index-url https://download.pytorch.org/whl/cu118