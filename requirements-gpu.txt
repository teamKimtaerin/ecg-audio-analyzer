# ECG Audio Analysis - GPU-Optimized Dependencies
# CUDA-accelerated libraries for AWS P3/G4 instances

# Include base requirements
-r requirements.txt

# GPU-Accelerated ML Models & Frameworks
torch>=2.0.0+cu118       # PyTorch with CUDA 11.8 support
torchaudio>=2.0.0+cu118  # GPU audio processing
torchvision>=0.15.0+cu118 # Vision models (for potential video analysis)

# Speaker Diarization & Speech Processing
pyannote-audio>=3.0.0    # GPU-accelerated speaker diarization
speechbrain>=0.5.0       # Additional speech AI capabilities
transformers[torch]>=4.30.0 # Hugging Face with CUDA optimization

# Emotion Analysis Dependencies
datasets>=2.12.0         # Hugging Face datasets for model fine-tuning
accelerate>=0.20.0       # Hugging Face acceleration library

# Audio Feature Extraction (GPU-optimized versions)
opensmile>=2.4.0         # Standardized acoustic features
librosa[gpu]>=0.10.0     # GPU-accelerated audio analysis

# NVIDIA CUDA Dependencies
nvidia-ml-py3>=7.352.0   # NVIDIA GPU monitoring
cupy-cuda11x>=12.0.0     # GPU-accelerated NumPy replacement (optional)

# Performance Profiling for GPU
gpustat>=1.1.0           # GPU monitoring utility
py-spy>=0.3.14           # Python performance profiler

# Additional GPU-optimized packages
--extra-index-url https://download.pytorch.org/whl/cu118

# Development tools for GPU environment
jupyter>=1.0.0           # For development and debugging on GPU instances
matplotlib>=3.7.0        # Visualization
seaborn>=0.12.0          # Statistical visualization